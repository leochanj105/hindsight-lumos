syntax = "proto3";

option go_package = "/datapb";

package datapb;

service Agent {
	rpc RemoteTrigger (Trigger) returns (TriggerReply) {}
}

service Coordinator {
	rpc LocalTrigger (Trigger) returns (TriggerReply) {}
	rpc Breadcrumbs (BreadcrumbsRequest) returns (BreadcrumbsReply) {}
}

message Trigger {
	int32 queue_id = 1;
	fixed64 base_trace_id = 2;
	repeated fixed64 trace_ids = 3;
}

message TriggerReply {
}

message Breadcrumbs {
	fixed64 trace_id = 1;
	repeated string addrs = 2;
}

message BreadcrumbsRequest {
	repeated Breadcrumbs breadcrumbs = 1;
}

message BreadcrumbsReply {
}